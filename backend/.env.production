# Production environment configuration for Collectify backend
# This file should be placed on the server or provided as environment variables in Azure

# CORS Configuration
# List of allowed origins for CORS requests
CORS_ORIGINS=https://black-sea-02a411d03.3.azurestaticapps.net,http://localhost:3000

# JWT Configuration
# Secret key for signing JWT tokens (use a strong, unique value in production)
JWT_SECRET_KEY=change_this_to_a_strong_random_value_in_production
JWT_ACCESS_TOKEN_EXPIRES=3600  # 1 hour
JWT_REFRESH_TOKEN_EXPIRES=2592000  # 30 days

# Database Configuration
# In production, consider using a managed database service
# For now, we're using SQLite but with an absolute path
DATABASE_URL=sqlite:////home/data/collectibles.db

# File Storage Configuration
UPLOAD_FOLDER=/home/data/uploads
